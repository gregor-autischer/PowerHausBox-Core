#!/usr/bin/env bash
set -euo pipefail

if ! command -v nsenter >/dev/null 2>&1; then
  echo "nsenter is not available in this add-on image." >&2
  exit 1
fi

TARGET_PID="${POWERHAUS_HOST_PID:-1}"

exec nsenter -t "${TARGET_PID}" -m -u -i -n -p -- ha "$@"
